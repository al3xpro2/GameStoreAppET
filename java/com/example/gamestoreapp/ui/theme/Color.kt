package com.example.gamestoreapp.ui.theme

import androidx.compose.ui.graphics.Color

// --- PALETA GAMER AZUL ---

// Colores Principales (Azul Neón Metálico del Logo)
val PrimaryBlue = Color(0xFF0077B6)       // Azul Profundo (Botones, AppBar)
val PrimaryDark = Color(0xFF00508B)       // Azul Oscuro para contraste
val AccentCyan = Color(0xFF00B4D8)        // Cyan Brillante (Acentos, Switches)

// Fondos Oscuros (Estilo Consola/PC)
val BackgroundDark = Color(0xFF0D1B2A)    // Fondo principal (Azul muy oscuro, casi negro)
val SurfaceDark = Color(0xFF1B263B)       // Fondo de tarjetas/superficies (Gris azulado oscuro)
val SurfaceLight = Color(0xFF415A77)      // Elementos destacados

// Textos
val TextWhite = Color(0xFFFFFFFF)        // Texto principal
val TextGray = Color(0xFFE0E1DD)         // Texto secundario

// Estados
val SuccessGreen = Color(0xFF38B000)      // Para mensajes de éxito o stock disponible
val ErrorRed = Color(0xFFD00000)          // Para errores o falta de stock